use the speckit-full skill in .agents/skills to implement the following features.
Follow all the point of the skill, once you opened the pull request in github

1. Run automata git get-pr-info --wait-finish-checks to wait for eventual problem in checks, then fix commit and push
